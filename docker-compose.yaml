version: '3.7'

services:
  api:
    container_name: angular
    build:
      context: .
      target: development
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    ports:
      - 4200:4200
    command: npm run start
    # networks:
    #   - desafio-gestao-produtos-network
